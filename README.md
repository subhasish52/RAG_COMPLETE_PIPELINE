# RAG_COMPLETE_PIPELINE
A complete Retrieval-Augmented Generation (RAG) pipeline built entirely from scratch without using frameworks like LangChain or LangGraph.  This project demonstrates how to design and implement a production-style RAG architecture by manually handling every stage of the pipeline from data ingestion to retrieval and response generation.
